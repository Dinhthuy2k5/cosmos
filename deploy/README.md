# Hướng dẫn deploy

## Yêu cầu
- Server Oracle Cloud (1 OCPU, 1GB RAM) đã cài Docker + Traefik
- DuckDNS domain: `astral.duckdns.org` trỏ về IP server

## Các bước deploy

### Trên máy local
```bash
npm run build